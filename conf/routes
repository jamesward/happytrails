# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.ApplicationController.index()

GET     /signup                     controllers.ApplicationController.signup()
POST    /signup                     controllers.ApplicationController.signup()

GET     /login                      controllers.ApplicationController.login()
POST    /login                      controllers.ApplicationController.login()

POST    /logout                     controllers.ApplicationController.logout()

GET     /addregion                  controllers.RegionController.saveRegion()
POST    /addregion                  controllers.RegionController.saveRegion()

GET     /:region/feed               controllers.RegionController.getRegionFeed(region)

POST    /:region/subscribe          controllers.RegionController.subscribe(region)
POST    /:region/unsubscribe        controllers.RegionController.unsubscribe(region)

GET     /:region/addroute           controllers.RegionController.saveRoute(region)
POST    /:region/addroute           controllers.RegionController.saveRoute(region)

DELETE  /:region                    controllers.RegionController.deleteRegion(region)

POST    /:region/:route/rate        controllers.RouteController.saveRating(region, route, rating: java.lang.Integer)

POST    /:region/:route/comment     controllers.RouteController.saveComment(region, route)

DELETE  /:region/:route             controllers.RouteController.deleteRoute(region, route)

GET     /:region/:route             controllers.RouteController.getRoute(region, route)

GET     /:region                    controllers.RegionController.getRegion(region)



# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
